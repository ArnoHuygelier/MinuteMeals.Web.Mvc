@model Recipe

<a href="recipe/@Model.Id">
    <div class="card flex-fill">
        <div class="card-body">
            <h5 class="card-title">@Model.Name</h5>
            <p class="card-text"><strong>Kooktijd:</strong> @Model.CookingTimeInMinutes minuten</p>
        </div>
    </div>
</a>